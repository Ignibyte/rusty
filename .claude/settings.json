{
  "_comment": "Guardrails for the rusty-workflow (CONSTITUTION.md). enforce-phase-gate: gated paths change only inside an implementing pipeline or under a waiver. enforce-secrets: no credential-looking bytes are written. enforce-commit-gate: no commit of gated files without a matching bin/gate.sh receipt, never --no-verify. Hooks are guardrails; the gate receipt is the proof.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/enforce-phase-gate.sh\"", "timeout": 10 },
          { "type": "command", "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/enforce-secrets.sh\"", "timeout": 10 }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/enforce-commit-gate.sh\"", "timeout": 20 }
        ]
      }
    ]
  }
}
